"""CLI for VaultTracker Scanner (pipeline wiring comes in a later plan step)."""


def main() -> None:
    print(
        "vaulttracker-scanner: pipeline not wired yet "
        "(discover → parse → normalize → validate → import). "
        "See VaultTrackerScanner/README.md and Documentation/Plans.",
    )
